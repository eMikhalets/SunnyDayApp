<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_pager_search"
        android:icon="@drawable/ic_search_24dp"
        android:title="@string/menu.view_pager.search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_pager_preference"
        android:icon="@drawable/ic_baseline_settings_24"
        android:title="@string/toolbar.menu.pref_title"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_pager_recreate_db"
        android:icon="@drawable/ic_baseline_update_24"
        android:title="@string/toolbar.menu.pref_title"
        app:showAsAction="ifRoom" />
</menu>